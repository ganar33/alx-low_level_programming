#include <stdio.h>

/**
 * main - Entry point
 *
 * step 1 : - Call "printf" to write the string
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
